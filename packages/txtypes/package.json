{
    "name": "@jchptf/txtypes",
    "version": "0.0.8",
    "description": "TODO",
    "main": "./dist/index.js",
    "module": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "typings": "./dist/index.d.ts",
    "typesVersions": {
        ">=3.3": {
            "dist": [
                "dist/ts3.3/*"
            ]
        },
        ">=3.2": {
            "dist": [
                "dist/ts3.2/*"
            ]
        },
        ">=3.1": {
            "dist": [
                "dist/ts3.1/*"
            ]
        }
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/jheinnic/portfolio-monorepo",
        "homepage": "https://github.com/jheinnic/portfolio-monorepo/tree/master/packages/txtypes"
    },
    "author": "John Heinnickel <jheinnic+npm@hotmail.com>",
    "license": "MIT",
    "keywords": [
        "es6",
        "typescript"
    ],
    "engines": {
        "node": ">=10.13.0"
    },
    "publishConfig": {
        "access": "public",
        "registry": "http://localhost:4873"
    },
    "scripts": {
        "build": "yarn run clean && tsc -b src && echo `pwd`",
        "clean": "tsc -b --clean && rm -rf dist build .nyc_output coverage doc && mkdir dist && mkdir build",
        "cover": "yarn test && nyc report --reporter=lcov",
        "doc": "node_modules/.bin/typedoc --mode file --module es2015 --target es6 --tsconfig tsconfig.json --externalPattern node_modules --excludeExternals --excludePrivate --out doc --gitRevision feature_bootstrap src typings",
        "pub": "yarn build && lerna publish --registry http://localhost:4873",
        "dtslint": "dtslint types",
        "test": "tsc -b test --clean && rm -rf build && mkdir build && tsc -b test && nyc mocha `find build -type f -name '*.js'`"
    },
    "devDependencies": {
        "@types/chai": "^4.1.7",
        "@types/mocha": "^5.2.5",
        "@types/node": "^10.12.18",
        "@types/sinon": "^7.0.2",
        "@types/sinon-chai": "^3.2.2",
        "chai": "^4.2.0",
        "dtslint": "^0.5.3",
        "mocha": "^5.2.0",
        "mocha-typescript": "^1.1.17",
        "nyc": "^12.0.2",
        "sinon": "^7.2.2",
        "sinon-chai": "^3.3.0",
        "ts-sinon": "^1.0.12",
        "tslint": "^5.11.0",
        "typedoc": "^0.14.2",
        "typescript": "3.2.2"
    },
    "dependencies": {
        "@jchptf/tupletypes": "^0.0.19",
        "@thi.ng/errors": "^0.1.12",
        "simplytyped": "^1.6.0",
        "tslib": "^1.9.3"
    }
}
