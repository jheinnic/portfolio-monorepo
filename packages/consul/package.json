{
    "name": "@jchptf/consul",
    "version": "0.0.4",
    "description": "TODO",
    "main": "./dist/index.js",
    "module": "./dist/index.js",
    "typings": "./dist/index.d.ts",
    "repository": {
        "type": "git",
        "url": "https://github.com/jheinnic/portfolio-monorepo",
        "homepage": "https://github.com/jheinnic/portfolio-monorepo/tree/master/packages/consul"
    },
    "author": "John Heinnickel <jheinnic+npm@hotmail.com>",
    "license": "MIT",
    "scripts": {
        "build": "yarn run clean && tsc -b src",
        "clean": "tsc -b --clean && rm -rf dist build .nyc_output coverage doc && mkdir dist && mkdir build",
        "cover": "yarn test && nyc report --reporter=lcov",
        "doc": "node_modules/.bin/typedoc --mode file --module commonjs --target es6 --tsconfig tsconfig.json --externalPattern node_modules --excludeExternals --excludePrivate --out doc --gitRevision feature_bootstrap src typings",
        "pub": "yarn run build && lerna publish --registry http://localhost:4873",
        "test": "tsc -b test --clean && rm -rf build && mkdir build && tsc -b test && nyc mocha `find build/test -type f -name '*.js'`"
    },
    "devDependencies": {
        "@types/chai": "^4.1.7",
        "@types/consul": "^0.23.33",
        "@types/mocha": "^5.2.5",
        "@types/node": "^10.12.18",
        "@types/sinon": "^7.0.2",
        "@types/sinon-chai": "^3.2.2",
        "chai": "^4.2.0",
        "lerna": "^3.8.0",
        "mocha": "^5.2.0",
        "mocha-typescript": "^1.1.17",
        "nyc": "^12.0.2",
        "sinon": "^7.2.2",
        "sinon-chai": "^3.3.0",
        "ts-sinon": "^1.0.12",
        "tslint": "^5.11.0",
        "typedoc": "^0.13.0",
        "typescript": "^3.1.4"
    },
    "engines": {
        "node": ">=10.13.0"
    },
    "dependencies": {
        "@jchptf/api": "^0.0.30",
        "@thi.ng/errors": "^0.1.12",
        "simplytyped": "^1.6.0",
        "tslib": "^1.9.3"
    },
    "keywords": [
        "es6",
        "typescript"
    ],
    "publishConfig": {
        "access": "public"
    }
}
